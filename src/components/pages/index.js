import React from 'react';
import Hero from '../Hero';
import Content from '../Content';

const index = () => {
    return (
        <div>
            <Hero/>
            <Content/>
        </div>
    );
};

export default index;